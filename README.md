# Multi-Sheet-Excel-Data-Extraction-and-Consolidation-in-R
This project implements an R-based pipeline to extract a target column from multiple Excel files containing hundreds of sheets, standardize inconsistent structures, and consolidate the results into a single dataset. The workflow was developed to support a research project to handle highly unstructured, manually entered data across sources.
Due to the confidentiality, the original data is not available.
